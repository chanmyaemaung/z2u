import { enTranslations } from '../translations/en';
import { myTranslations } from '../translations/my';
import { Translations } from '../types/translation.types';

export const TRANSLATIONS: Translations = {
  en: enTranslations,
  my: myTranslations,
};
